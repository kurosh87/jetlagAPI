specVersion: "v1beta"
serviceId: "jetlag-api"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "jetlag-api-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
